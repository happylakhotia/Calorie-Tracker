# ── NutriTrack Frontend Environment Configuration ──

# Backend API base URL for production (e.g., https://nutritrack-api.onrender.com/api)
# In local development, leave blank to use the Vite dev server proxy (/api -> http://localhost:5000)
VITE_API_URL=
